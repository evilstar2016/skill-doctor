# Dev Log: 2026-05-14

## Status
Branch: feature/f2-remediation-text
Active Feature: F2 — Suggested remediation text / LLM remediation output
Phase: implemented → tested → ready to commit

## Commits

| SHA | Message | Requirement | Decisions |
| --- | --- | --- | --- |
| `ddc1f9c` | feat: batch and harden LLM explanation calls | F4 hardening | [AI] Kept the OpenAI-compatible LLM path structured and cheaper before reusing the same analysis surface for remediation output. |
| `7d086d3` | fix: clean up type annotations in embeddingProvider | F2 follow-up | [AI] Fixed the typing issue in-place instead of widening types elsewhere. |
| `45fa4dc` | chore: bump version to 0.2.0 | F3 release flow | [human] Requested releasing the current version before starting the next roadmap item. |
| (uncommitted) | F2 remediation text branch: add `ConflictPair.remediation`, terminal/HTML rendering, then switch `--analyze` to accept LLM remediation with rule-based fallback | F2 roadmap item: Suggested remediation text | [AI] Reused the existing optional analysis pipeline instead of adding a new CLI flag or a second LLM client; [AI] Kept rule-based remediation as fallback when `--analyze` is off or the model omits remediation text. |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today

- Released the current code as `v0.2.0` and pushed the tag before starting the next roadmap item.
- Started `feature/f2-remediation-text` and added `ConflictPair.remediation` so plain text / HTML / JSON outputs can surface actionable fix suggestions.
- Verified that the first remediation implementation was rule-based only, not LLM-generated.
- Extended the existing F2 optional AI analysis path so `--analyze` can now return `analysis.remediation`, and the final `pair.remediation` prefers the LLM sentence when present.
- Preserved non-LLM behavior: when `--analyze` is disabled or the model does not return a remediation string, the existing local rule-based remediation still fills the field.
- Added regression coverage for prompt schema, analysis parsing, remediation selection priority, and end-to-end `detectConflicts` wiring.
- Validation: targeted remediation tests passed (`22` tests), full suite passed (`194` tests), and `npm run build` passed.

## Progress State

The last remaining F2 roadmap item is now implemented in code on `feature/f2-remediation-text`. Conflict output already had a remediation field and renderers on this branch, and this session completed the missing part: LLM-generated fix suggestions now flow through the existing `--analyze` pipeline without changing the default non-LLM path. The branch is tested and ready for review/commit, while the roadmap file itself has not been updated yet.

## Next Session

**Pick up at**: Run one real-model smoke test with `npx skill-doctor conflicts --strategy embedding --analyze --json`, confirm the returned `remediation` text quality, then commit the branch.
**Context**: LLM remediation is only expected when `--analyze` is enabled and `analysis.baseUrl` + `analysis.model` are configured in `~/.skill-doctor/config.json`; otherwise the rule-based fallback remains active. `.claude/settings.local.json` is locally modified and should not be committed.
