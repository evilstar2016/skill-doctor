# Dev Log: 2026-05-14

## Status
Branch: main
Active Feature: F5 — Trust/provenance metadata + F4/F5 cache/performance hardening
Phase: implemented → verified → ready to commit

## Commits

| SHA | Message | Requirement | Decisions |
| --- | --- | --- | --- |
| `ddc1f9c` | feat: batch and harden LLM explanation calls | F4 hardening | [AI] Kept the OpenAI-compatible JSON LLM path shared and structured before reusing it for later provenance/explanation calls. |
| `7d086d3` | fix: clean up type annotations in embeddingProvider | F2 follow-up | [AI] Fixed the typing issue in place instead of widening adjacent types. |
| `45fa4dc` | chore: bump version to 0.2.0 | F3 release flow | [human] Requested cutting the current version before moving on to the next roadmap slice. |
| `874370a` | feat: add conflict remediation suggestions | F2 — Suggested remediation text | [AI] Reused the existing optional analysis path so remediation stays structured when `--analyze` is enabled and rule-based when it is not. |
| `d9a35bf` | feat: add skill provenance metadata and cache lookups | F5 — Trust/provenance metadata; F4/F5 hardening follow-up | [joint] Corrected provenance precedence to skill-dir git → skill-dir metadata → frontmatter → LLM and rejected workspace git leakage for nested skills; [AI] Added persistent provenance cache plus a `resolved` marker so partial LLM results do not trigger repeated retries; [AI] Switched `when-to-use.json` / `group-labels.json` to BOM-safe UTF-8 for Windows-friendly Chinese output; [AI] Reproduced `show zoom-out` in the real repo before fixing it and capped JSON LLM requests with a 4s timeout. |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today

- Released the current code as `v0.2.0` and completed the F2 remediation text path so conflict analysis can emit actionable fix suggestions.
- Implemented F5 trust/provenance metadata in `scan`, `show`, and `audit`, surfacing source repository, author, install source, scope, and confidence.
- Corrected the provenance resolution order to prefer the skill directory’s own git metadata, then skill-dir metadata files, then frontmatter, and only then LLM extraction.
- Prevented nested project skills from inheriting the enclosing workspace repo as their provenance source.
- Propagated the async provenance path through parsing, discovery, CLI entrypoints, renderers, and tests, and updated roadmap/spec documentation to mark F5 provenance as done.
- Fixed Windows cache encoding so `when-to-use.json` and `group-labels.json` preserve Chinese text when opened with Windows-default tooling.
- Reproduced the user-reported `npx skill-doctor show zoom-out` slowness in the real repository, found the remaining root cause (partial provenance cache entries retried forever), and fixed it with persistent provenance caching, a `resolved` sentinel, and a hard LLM request timeout.
- Validation: targeted provenance/cache/CLI suites passed, full `npm test` passed (`212` tests), `npm run build` passed, and real command timing dropped from ~58–107s (`node dist/index.cjs show zoom-out`) to ~0.105s; `npx skill-doctor show zoom-out` measured ~1.77s with most of the remainder in `npx` startup.

## Progress State

All roadmap items previously marked `next` are now complete across F2, F4, F5, and F6. The codebase now has provenance-aware inventory/audit output, persistent explanation/group/provenance caches, and a verified fast path for `show` that avoids dragging the whole repo through repeated LLM lookups. What remains is to choose the next `later` slice to move forward—most naturally an extension of the newly completed F5 audit layer or a higher-level explanation/consolidation feature.

## Next Session

**Pick up at**: Implement F5 “Security report output” — the minimal vertical slice is:
1. Add `--report [path]` flag to the `audit` command.
2. Add a new report renderer that consumes `AuditResult` directly (reuse `runAudit()`).
3. Report sections: scan total, high/med/low summary, findings table, provenance fields.
4. Leave existing `audit` plain-text and `--json` output unchanged.

**Rationale** (human): Tightest connection to the just-landed F5 provenance work; reuses the `scan --report` write-file pattern already in the codebase; no mutation policy or platform write-back needed (unlike F6 enable/disable); fastest path to a shareable, archivable security result.

**Tradeoff**: This is a presentation-layer enhancement — it does not change local skill state the way F6 enable/disable would, but it completes the F5 audit story and leaves a clean handoff point for F6.

**Context**: The current audit pipeline already includes structured findings plus provenance fields, so report output can reuse `runAudit`, `renderAudit`, and the existing report-writing pattern instead of inventing a second analysis path. Keep `.claude/settings.local.json` out of commits.
