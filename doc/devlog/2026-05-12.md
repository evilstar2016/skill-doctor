# Dev Log: 2026-05-12

## Status
Branch: progress/f2-embedding-conflicts-20260512
Active Feature: F2 — Embedding-Based Conflict Detection
Phase: implementing → ready for smoke test

## Commits

| SHA | Time | Message | Requirement | Decisions |
|-----|------|---------|-------------|-----------|
| `95c4592` | 19:02 | Implement configurable embedding conflict detection | F2 Slice 1, Slice 2 | [AI] Cache key = provider+model+textHash; config read from `~/.skill-doctor/config.json`; [AI] EmbeddingCache interface in types rather than concrete class |
| `46094e9` | 21:03 | Implement Slice 3 (Optional AI Analysis) and Slice 5 (Calibration) | F2 Slice 3 (Task 3.1, 3.2), F2 Slice 5 (Task 5.1, 5.2) | [AI] Used native `fetch` for Ollama client (no SDK dep); [AI] Silent error handling for analysis — optional feature must not break base detection; [AI] Mock-vector calibration tests instead of live API |

## Completed Today
- F2 Slice 1: Conflict strategy skeleton — `detectConflicts` now routes to `token` or `embedding` strategy
- F2 Slice 2: Local embedding API provider + JSON cache — reads `~/.skill-doctor/config.json`
- F2 Slice 3: Optional AI analysis — `buildAnalysisPrompt` + `analyzeConflict` (Ollama/OpenAI-compat), `--analyze` CLI flag
- F2 Slice 4: Updated render/report output (terminal + HTML)
- F2 Slice 5: Calibration fixtures (positive pair + adjacent pair) with mock-vector threshold tests
- 117 tests passing, `npm run build` clean

## Progress State
All 5 slices of F2 are complete. The branch `progress/f2-embedding-conflicts-20260512` is pushed.
Only remaining item before PR: live smoke test using a real local embedding API
(`~/.skill-doctor/config.json` must be configured with `embedding.baseUrl` and `embedding.model`).

## Next Session
**Pick up at**: Run live smoke test — configure `~/.skill-doctor/config.json`, then run:
```
npx skill-doctor conflicts --strategy embedding --json
npx skill-doctor conflicts --strategy embedding --analyze --json   # if Ollama available
```
Compare results against `--strategy token`. If false positive rate is acceptable, open PR to merge F2 into main.
