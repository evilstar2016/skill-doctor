# Dev Log: 2026-05-13

## Status
Branch: main
Active Feature: F4 Explanation Layer (LLM + group label cache)
Phase: implemented → tested (pending commit)

## Commits

| SHA | Message | Requirement | Decisions |
|-----|---------|-------------|-----------|
| `e6c62e3` | chore: add devlog 2026-05-12 (F2 Slice 1-5 complete) | F2 Slice 1-5 | [AI] Captured the branch as functionally complete on F2 so follow-up work could focus on verification and polish |
| `dac61fd` | Add F2 scenario validation workflow | F2 scenario | [AI] Landed parser/discovery hardening and scenario workflow directly onto main after stash cleanup |
| `515a590` | Harden discovery path resolution and skill parsing | F2 follow-up | [AI] Hardened YAML block scalar frontmatter parsing and path resolution as a follow-up to F2 scope cleanup |
| `18ec141` | Implement F5 AC6 — add ANSI color output to audit command | F5 AC6 | [AI] Raw ANSI escape codes with TTY detection; zero new deps |
| `7f8ab54` | Implement F6 duplicate cleanup suggestions | F6 | [AI] `suggestCleanup` takes injectable `statFn` for test isolation |
| `c5fb8f6` | Implement F6 ignore list — suppress false positives via config.json | F6 | [AI] Extended existing config rather than new file; filtering applied before limit/kind |
| (uncommitted) | F4 LLM explanation + group label cache | F4 | [joint] LLM primary, token as fallback — user's explicit requirement |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today

### Session 1 — F5 AC6 + F6 duplicate cleanup
- F5 AC6: TTY-aware ANSI color output in `renderAudit.ts`; zero runtime deps
- F6: `suggestCleanup.ts` with injectable `statFn`; `renderConflicts` extended with optional SUGGESTIONS block

### Session 2 — F6 ignore list
- `loadUserConfig` extended with `IgnoreUserConfig { skillNames?, conflictPairs? }`
- `applyIgnoreList.ts`: `filterConflicts` + `filterFindings`, sort-normalized pair matching
- 157 tests passing

### Session 3 — F4 LLM Explanation Layer
- **F4.1 `show` enhancement**: `buildExplanation` async + optional `LlmExplainOptions`; `llmWhenToUse` generates 1-2 sentence WHEN TO USE via LLM; `renderShow` shows LLM text above triggers when present
- **F4.2 `scan --group`**: `groupSkills` async + `GroupSkillsOptions { llmOptions?, labelCache? }`; `llmGroupLabel` generates semantic group names; LLM falls back to token label on error
- **Group label cache** (`~/.skill-doctor/group-labels.json`): `groupLabelCache.ts` with `clusterKey` (sorted `sourcePath`s), `loadGroupLabelCache`, `saveGroupLabelCache`; CLI loads before / saves after each `scan --group` run; second run is instant
- **Config separation**: `readLlmExplainOptions()` only reads `config.analysis` (not embedding); no LLM config → token-only mode, no error
- 173 tests passing (11 new cache tests + 2 cache-aware groupSkills tests)

## Progress State
F4 LLM layer is functionally complete and smoke-tested. `show` generates LLM "when to use" paragraphs. `scan --group` generates semantic group names backed by `~/.skill-doctor/group-labels.json` cache — first run calls LLM for new clusters only; subsequent runs return instantly for known clusters. Config: `analysis.baseUrl + analysis.model` in config.json activates LLM; absent → silent token fallback. Code uncommitted.

## Next Session
**Pick up at**: Commit the F4 LLM layer work (`git add src/explain/ tests/explain/ src/cli/index.ts src/types/explain.ts && git commit -m "feat: F4 LLM explanation layer with group label cache"`), then update the roadmap to mark F4 items as `done`.
**Context**: 173 tests green. Cache file lives at `~/.skill-doctor/group-labels.json`. LLM config reads from `config.analysis` only (not embedding). Local test model: Qwen3-32B at `http://10.100.129.193:30003/v1`.

|-----|---------|-------------|-----------|
| `e6c62e3` | chore: add devlog 2026-05-12 (F2 Slice 1-5 complete) | F2 Slice 1-5 | [AI] Captured the branch as functionally complete on F2 so follow-up work could focus on verification and polish |
| `dac61fd` | Add F2 scenario validation workflow | F2 scenario | [AI] Landed parser/discovery hardening and scenario workflow directly onto main after stash cleanup |
| `515a590` | Harden discovery path resolution and skill parsing | F2 follow-up | [AI] Hardened YAML block scalar frontmatter parsing and path resolution as a follow-up to F2 scope cleanup |
| `18ec141` | Implement F5 AC6 — add ANSI color output to audit command | F5 AC6 | [AI] Raw ANSI escape codes with TTY detection; zero new deps |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today

### Session 1 — F5 AC6 color output
- Reviewed full project state: all 131 tests green, F2+F3+F4 done on main, F5 code shipped but AC6 (color output) missing
- Implemented AC6 in `src/render/renderAudit.ts`: module-level `useColor = process.stdout.isTTY === true`, inline red/yellow/gray helpers, `padEnd` applied before colorizing to preserve column alignment
- Decision: used raw ANSI escape codes instead of adding a dependency — project has zero runtime deps [AI]
- Ran fixture smoke-test: `audit --scope project` correctly reported 3 findings (HIGH destructive, MED secret-leak, LOW network-call), then removed fixture
- Updated roadmap: F5 both items → `done`; Current Recommendation → F6

### Session 3 — F6 ignore list / allow list
- Implemented `src/types/cleanup.ts` is already done; extended `SkillDoctorUserConfig` in `loadUserConfig.ts` with `IgnoreUserConfig { skillNames?, conflictPairs? }` and a `normalizeIgnoreConfig` parser with strict type validation (strips non-strings from arrays, filters malformed pairs)
- Created `src/config/applyIgnoreList.ts`: `filterConflicts` removes pairs where either skill is ignored or the exact pair is in `conflictPairs` (sort-normalized); `filterFindings` removes audit findings for ignored skill names
- Decision: extended the existing `~/.skill-doctor/config.json` rather than adding a separate `ignore.json` — one file, one load, consistent with existing embedding/analysis config [AI]
- Decision: ignore filtering happens before kind-filtering and limit in `conflicts`, and before severity-filtering in `audit` — both commands now load user config and apply ignore at the top of their handler [AI]
- 15 new tests (11 unit + 2 config parsing + 2 integration); total: 142 → 157 all green
- Updated roadmap: F6 "Ignore list" → `done`; all F6 `next` items are complete

- Implemented `src/types/cleanup.ts` (`CleanupSuggestion` type), `src/cleanup/suggestCleanup.ts` (pure fn, injectable `statFn` for test isolation)
- Decision: `suggestCleanup` takes a `statFn` parameter defaulting to `fs.statSync` — enables deterministic tests with fake mtimes; real FS only touched at CLI callsite [AI]
- Decision: suggestions appended automatically whenever duplicate pairs are present — no new flag; the extra output is always useful and not noisy [AI]
- Updated `renderConflicts(pairs, suggestions?)` — optional param keeps existing callers backward-compatible; SUGGESTIONS block only rendered when `suggestions.length > 0`
- Added `suggestions` field to `buildConflictsPayload` and `conflicts --json` output
- 11 new tests (7 unit + 2 render + 2 integration); total: 131 → 142 all green
- Updated roadmap: F6 "Duplicate cleanup suggestions" → `done`; next → "Ignore list / allow list"

## Progress State
F5 and all planned F6 items are done. `conflicts` output now includes SUGGESTIONS and respects `ignore.skillNames`/`ignore.conflictPairs` from config. `audit` respects `ignore.skillNames`. The codebase has 157 passing tests and the roadmap has no remaining `next` items — only `later` items remain (enable/disable workflow, merge/consolidate suggestions, cross-skill overlap narratives, trust/provenance metadata).

## Next Session
**Pick up at**: All `next` items are complete. The next development cycle should be planned from the `later` items or new requirements. Possible candidates: (1) F4 cross-skill overlap narratives, (2) F5 trust/provenance metadata, (3) F6 enable/disable workflow.
**Context**: 157 tests pass. Roadmap has no remaining `next` items. Config file lives at `~/.skill-doctor/config.json` — it now supports `embedding`, `analysis`, and `ignore` sections.
