# Dev Log: 2026-05-13

## Status

Branch: main
Active Feature: F4 Explanation Layer and LLM integration hardening
Phase: implemented → tested → ready to ship

## Commits

| SHA | Message | Requirement | Decisions |
| --- | --- | --- | --- |
| `dac61fd` | Add F2 scenario validation workflow | F2 scenario | [AI] Added a feature-scenario workflow instead of widening generic test scripts so embedding behavior could be verified end-to-end. |
| `515a590` | Harden discovery path resolution and skill parsing | F2 follow-up | [AI] Fixed parser and path handling in the owning code path rather than layering exceptions into discovery callers. |
| `18ec141` | Implement F5 AC6 — add ANSI color output to audit command | F5 AC6 | [AI] Used raw ANSI escapes with TTY detection instead of adding a runtime dependency. |
| `7f8ab54` | Implement F6 duplicate cleanup suggestions | F6 | [AI] Kept cleanup suggestions pure with injectable `statFn` so ranking behavior stayed testable. |
| `c5fb8f6` | Implement F6 ignore list — suppress false positives via config.json | F6 | [AI] Extended the existing user config instead of creating a second config surface for ignore rules. |
| `2c39357` | feat: F4 LLM explanation layer with persistent cache for show and scan --group | F4 | [joint] Shipped LLM-first explanations and grouping with persistent caches, while keeping token-based fallback when no config or no response is available. |
| (uncommitted) | F4 follow-up: structured JSON LLM calls, batched group labeling, failure diagnostics, roadmap/devlog refresh | F4 | [joint] Replaced free-form prompt parsing with JSON responses and batched uncached group requests so provider behavior is easier to reason about and rate-limit symptoms are visible. |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today

- F2 follow-up: landed scenario validation workflow plus parser/path hardening so embedding conflict detection has repeatable coverage.
- F5 AC6: shipped TTY-aware ANSI color output in `audit` without adding dependencies.
- F6: shipped duplicate cleanup suggestions and ignore-list filtering via `~/.skill-doctor/config.json`; related tests are green.
- F4 core: shipped `show` WHEN TO USE explanations, RELATED SKILLS, `scan --group`, persistent caches in `~/.skill-doctor/group-labels.json` and `~/.skill-doctor/when-to-use.json`, and README config documentation.
- F4 hardening: investigated MiniMax OpenAI-compatible integration, confirmed provider-side `429` responses, added visible fallback warnings, switched LLM parsing to structured JSON, and batched uncached group-label requests into one call.
- Validation: explain-related tests pass (`26` tests across `llmExplain`, `buildExplanation`, and `groupSkills`) and `npm run build` passes.

## Blockers / Open Questions

- MiniMax OpenAI-compatible endpoint returned `429 usage limit exceeded` while identifying the key under a lower-tier token plan. The current client behavior is now explicit, but provider-side quota semantics still need confirmation.

## Progress State

F2, F5, and F6 planned items are complete. F4 is now functionally shipped: explanations and workflow grouping are in the CLI, caches persist across runs, and the LLM path is stricter and cheaper because it uses JSON responses and batched group labeling. The remaining gap is not local implementation but provider validation: once a working analysis key or confirmed quota path is available, `when-to-use.json` should start filling from successful responses.

## Next Session

**Pick up at**: Re-test `npx skill-doctor show <skill>` and `npx skill-doctor scan --scope project --group` with a confirmed-working analysis key, then verify that `~/.skill-doctor/when-to-use.json` is populated and decide whether to expose cache/request stats in CLI output.
**Context**: `main` contains committed F4 core work plus uncommitted F4 hardening/docs updates. `scan --group` now batches uncached groups into one LLM request. Local config lives outside the repo in `~/.skill-doctor/config.json` and should not be committed.
