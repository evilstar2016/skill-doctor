# Dev Log: 2026-05-13

## Status
Branch: progress/f2-embedding-conflicts-20260512
Active Feature: F2 — embedding conflict detection + scenario workflow
Phase: implemented → validated → merge-prep

## Commits

| SHA | Message | Requirement | Decisions |
|-----|---------|-------------|-----------|
| `e6c62e3` | chore: add devlog 2026-05-12 (F2 Slice 1-5 complete) | F2 Slice 1-5 | [AI] Captured the branch as functionally complete on F2 so follow-up work could focus on verification and polish |

> Decision prefix: [AI] = made by Claude, [human] = made by developer, [joint] = discussed

## Completed Today
- Audited branch progress against `doc/devlog/2026-05-12.md`, `doc/specs/skill-doctor-f2-embedding-conflict-spec.md`, and `doc/specs/skill-doctor-f2-embedding-conflict-tasks.md`
- Re-verified branch health: `npm test` passed and `npm run build` passed, then later re-ran them again after scope cleanup and scenario-workflow wiring
- Smoke-ran `node dist/index.js scan --json` and found an active parser gap: official/frontmatter `description: >` block scalars were being parsed as literal `>` in several scanned skills
- Completed the parser follow-up locally: `src/parsing/parseSkill.ts` now parses YAML block scalar frontmatter for `description`, `when_to_use`, and `applyTo`, with regression coverage in `tests/parsing/parseSkill.test.ts`
- Re-ran a real scan and confirmed previously broken samples (`agent-evaluator`, `project-analysis`, `project-ir-skill`, `devlog`) now expose full descriptions instead of `>`
- Verified the user-provided local embedding service end-to-end: `http://10.100.129.193:30003/v1` with model `bge-m3` returned a live 1024-d embedding vector via direct provider probe, and `conflicts --strategy embedding --json` executed successfully against the real local skill set
- Built an isolated synthetic workspace with 4 fake skills (3 intentionally near-duplicate readiness-review skills + 1 migration control) and confirmed the expected behavior: token strategy reported 0 conflicts while embedding strategy reported a 3-edge conflict cluster among the three semantically similar skills with no false positive on the migration control
- Added a durable F2 scenario tree under `doc/scenarios/f2-embedding-conflict/` with manifest, IT/slice scenario docs, and evidence files so validation now has a stable home in-repo
- Extracted a shared CLI harness, added a feature-scoped scenario runner plus manifest validation, and implemented the first executable F2 semantic-cluster scenario under `tests/scenarios/f2-embedding-conflict/`
- Added a project-scoped Claude hook wrapper in `.claude/settings.json` and verified the post-unit-test auto-chain by piping a synthetic `PostToolUse` Bash payload into `scripts/testing/post-unit-test-hook.cjs`
- Narrowed the current merge path back to F2 + scenario workflow by stashing parser/discovery hardening, local settings, and generated artifacts into `stash@{0}` (`split: parser-discovery hardening and local artifacts from F2 PR`) for a separate follow-up branch/PR

## Blockers / Open Questions
- No functional blockers remain on the scoped F2 + scenario workflow path: `npm test`, `npm run build`, `npm run test:scenarios -- --feature f2-embedding-conflict`, and `npm run verify:feature -- --feature f2-embedding-conflict --record` are green.
- Parser/discovery hardening, local settings, and generated artifacts were intentionally removed from the merge path and preserved in `stash@{0}` (`split: parser-discovery hardening and local artifacts from F2 PR`) for a separate follow-up branch/PR.

## Progress State
F2 is now validated at four levels on this branch: automated tests are green, the build is green, the real user-provided local embedding service is reachable and returns valid vectors, and the new feature-scoped scenario workflow passes through both direct scenario execution and the full `verify:feature` path. That gives stronger evidence that the embedding path is working as intended rather than only passing mocks, and it leaves behind a reusable spec → scenario → evidence workflow for future features. The branch has also been narrowed back to F2 + scenario workflow, with parser/discovery hardening preserved separately in `stash@{0}` for follow-up work instead of being mixed into this PR.

## Next Session
**Pick up at**: Create the scoped F2 + scenario workflow commit/PR, then revive `stash@{0}` onto its own branch if you still want to ship the parser/discovery hardening follow-up.
**Context**: Final local gates are green: `npm test`, `npm run build`, `npm run test:scenarios -- --feature f2-embedding-conflict`, and `npm run verify:feature -- --feature f2-embedding-conflict --record`. Scenario evidence now lives under `doc/scenarios/f2-embedding-conflict/evidence/`, and the follow-up non-F2 work is preserved in stash message `split: parser-discovery hardening and local artifacts from F2 PR`.