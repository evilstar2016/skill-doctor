# 扫描与发现 模块测试用例

## 1. 基本信息
- 测试对象：skill-doctor「扫描与发现」功能模块
- 模块编号前缀：**SD**（Scan & Discovery）
- 用例类型：功能测试 / 边界测试 / 异常测试 / 配置测试
- 适用场景：技能发现（resolvePaths/scanSkills/detectAgents）、SKILL.md 解析（parseSkill）、扫描路径配置（scanSources）、健康检查编排（runHealthCheck）、scan/show 命令
- 对应测试观点：见 `01-扫描与发现-测试观点.md`（编号 SD-TP-xx）

## 2. 自动化说明

- 本模块 34 条用例（含新增的 SD-TC-34）**全部**是纯函数 / CLI 子进程级别的行为验证，均已落地为 Vitest 自动化脚本，不存在需要人工操作 UI 或凭感官判断的用例。
- 每条用例标注：
  - **自动化状态**：`✅ 已自动化（已有）` = 仓库中已存在对应脚本；`✅ 已自动化（本轮新增）` = 本轮为覆盖缺口新写的脚本。
  - **测试脚本**：脚本文件路径 + `describe`/`it` 标题，可直接按此定位。
- 运行方式：
  - 纯单元测试（discovery/parsing/config/application 目录）：`npx vitest run tests/discovery tests/parsing tests/config tests/application`
  - CLI 集成测试（会先 `npm run build` 再以子进程方式跑编译产物）：`npx vitest run tests/cli/integration.test.ts`（`describe.skipIf(process.platform === 'win32')`，当前在 Windows 开发机上会跳过，CI 的 ubuntu-latest 环境会执行；本轮新增用例已在本机临时移除 skip 后手工跑通验证，详见文末说明）

## 3. 用例列表

### SD-TC-01 扫描单个 Claude 项目级技能
- 关联测试观点：SD-TP-01、SD-TP-19
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan finds workspace skills from a controlled project directory')`
- 用例标题：scan 命令正确发现单个项目级 Claude 技能
- 前置条件：临时工作目录、临时 HOME 目录均为空
- 测试数据：
  - 路径：`<cwd>/.claude/skills/karpathy-guidelines/SKILL.md`
  - frontmatter：`name: karpathy-guidelines`，`description: avoid overengineering`
- 操作步骤：
  1. 在工作目录写入上述 SKILL.md
  2. 执行 `skill-doctor scan`（cwd 与 home 指向临时目录）
- 预期结果：
  - 退出码为 0
  - 输出包含 `Total skills installed: 1`
  - 输出包含平台标识 `claude`

### SD-TC-02 scan --json 结构化输出校验
- 关联测试观点：SD-TP-01、SD-TP-21
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan --json accepts explicit token strategy flags')`
- 用例标题：scan --json 返回准确的 summary 与 skills 数组
- 前置条件：同 SD-TC-01 的技能文件
- 测试数据：追加 `--strategy token --threshold 0.8 --embedding-model local-model --json`
- 操作步骤：
  1. 写入 karpathy-guidelines 技能
  2. 执行 `skill-doctor scan --strategy token --threshold 0.8 --embedding-model local-model --json`
  3. 解析 stdout 为 JSON
- 预期结果：
  - `status` 为 0
  - `payload.summary.totalSkillsInstalled === 1`
  - `payload.summary.duplicatesDetected === 0`，`conflictsDetected === 0`
  - `payload.summary.scopes` 为 `{ project: 1 }`
  - `payload.summary.platformsByScope` 为 `{ project: { claude: 1 } }`
  - `payload.skills[0].name === 'karpathy-guidelines'`

### SD-TC-03 global 与 project 技能同时存在时的 scope 分桶
- 关联测试观点：SD-TP-18、SD-TP-21
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan --json includes scope buckets when both global and project skills exist')`
- 用例标题：scan --json 正确区分 global/project scope 统计
- 前置条件：工作目录与 home 目录均可写
- 测试数据：
  - project：`<cwd>/.claude/skills/project-skill/SKILL.md`
  - global：`<home>/.claude/skills/global-skill/SKILL.md`
- 操作步骤：
  1. 分别写入两个技能文件
  2. 执行 `skill-doctor scan --json`
- 预期结果：
  - `payload.summary.scopes` 等于 `{ global: 1, project: 1 }`
  - `payload.summary.platformsByScope` 等于 `{ global: { claude: 1 }, project: { claude: 1 } }`

### SD-TC-04 scan --scope project 过滤 global 技能
- 关联测试观点：SD-TP-18
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan --scope project excludes global skills from the summary')`
- 用例标题：--scope project 应从统计与列表中排除 global 技能
- 前置条件：同时存在 project 与 global 技能
- 测试数据：同 SD-TC-03
- 操作步骤：
  1. 写入 project 与 global 技能
  2. 执行 `skill-doctor scan --scope project`
- 预期结果：
  - 输出中的技能总数只统计 project scope
  - 不出现 global-skill 相关条目

### SD-TC-05 scan --scope 非法值报错
- 关联测试观点：SD-TP-18
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan rejects an invalid --scope value')`
- 用例标题：非法 scope 参数应报错并非零码退出
- 前置条件：无
- 测试数据：`--scope other`
- 操作步骤：执行 `skill-doctor scan --scope other`
- 预期结果：
  - 进程以非零码退出
  - stderr 提示 `Invalid scope. Use --scope project|global|all`
- 新增测试代码：
  ```ts
  // SD-TC-05
  it('scan rejects an invalid --scope value', () => {
    const root = createTempRoot();
    const cwd = join(root, 'workspace');
    const home = join(root, 'home');
    mkdirSync(cwd, { recursive: true });

    const result = runCli(['scan', '--scope', 'other'], cwd, home);

    expect(result.status).toBe(1);
    expect(result.stderr).toContain('Invalid scope. Use --scope project|global|all');
  });
  ```

### SD-TC-06 show 命令查看存在的技能
- 关联测试观点：SD-TP-19
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('show prints the details of an existing skill and fails for a missing one')`
- 用例标题：show 打印已存在技能的详情
- 前置条件：已写入 karpathy-guidelines 技能文件（同 SD-TC-01）
- 测试数据：技能名 `karpathy-guidelines`
- 操作步骤：执行 `skill-doctor show karpathy-guidelines`
- 预期结果：
  - 退出码为 0
  - 输出包含 `SKILL: karpathy-guidelines`

### SD-TC-07 show 命令查看不存在的技能
- 关联测试观点：SD-TP-19
- 自动化状态：✅ 已自动化（已有，与 SD-TC-06 同一用例内的第二个断言分支）
- 测试脚本：`tests/cli/integration.test.ts` → `it('show prints the details of an existing skill and fails for a missing one')`
- 用例标题：show 对不存在技能报错
- 前置条件：技能库中不存在该技能名
- 测试数据：技能名 `missing-skill`
- 操作步骤：执行 `skill-doctor show missing-skill`
- 预期结果：
  - 退出码为 1
  - stderr 包含 `Skill not found: missing-skill`

### SD-TC-08 show --json 返回结构化单条记录
- 关联测试观点：SD-TP-19、SD-TP-21
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('show --json returns a single structured skill record')`
- 用例标题：show --json 输出符合 SkillRecord 结构
- 前置条件：已写入 karpathy-guidelines 技能文件
- 测试数据：`show karpathy-guidelines --json`
- 操作步骤：执行命令并解析 JSON
- 预期结果：
  - `payload.name === 'karpathy-guidelines'`
  - `payload.platform === 'claude'`
  - `payload.scope === 'project'`

### SD-TC-09 show 输出 WHEN TO USE 与 relatedSkills
- 关联测试观点：SD-TP-13、SD-TP-19
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/cli/integration.test.ts` → `it('show includes WHEN TO USE section with triggers')`、`it('show --json includes relatedSkills array')`
- 用例标题：show 展示 triggers 汇总的 WHEN TO USE 小节，及 --json 下的 relatedSkills 数组
- 前置条件：SKILL.md 含 `## When to Use` 小节的多条 bullet
- 测试数据：技能正文包含 `## When to Use` 下 2 条以上 `- ` 开头的触发说明
- 操作步骤：
  1. 执行 `show <name>`（终端模式）
  2. 执行 `show <name> --json`
- 预期结果：
  - 终端输出包含 `WHEN TO USE` 段落及对应触发词
  - JSON 输出包含 `relatedSkills` 数组字段（可为空数组）

### SD-TC-10 SKILL.md 目录识别 — 仅 SKILL.md 才算技能
- 关联测试观点：SD-TP-02
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('skips a skill dir that has .md files but none named SKILL.md')`
- 用例标题：目录下只有 README.md（无 SKILL.md）时不应被识别为技能
- 前置条件：`.claude/skills/my-skill/` 下只写 `README.md` 与 `README.en.md`
- 测试数据：同上
- 操作步骤：调用 `resolvePaths(cwd, { homeDir })` 并过滤 `platform === 'claude'`
- 预期结果：结果长度为 0

### SD-TC-11 SKILL.md 目录识别 — 多个 .md 共存时优先选 SKILL.md
- 关联测试观点：SD-TP-02
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('picks SKILL.md when a skill dir contains multiple md files')`
- 用例标题：技能目录同时存在 SKILL.md、README.md、README.en.md 时只取 SKILL.md
- 前置条件：`.claude/skills/my-skill/` 下写入三个 .md 文件
- 测试数据：同上
- 操作步骤：调用 `resolvePaths`
- 预期结果：
  - claude 平台结果长度为 1
  - 该结果的 filePath 包含 `SKILL.md`

### SD-TC-12 支持子目录不产生额外技能条目
- 关联测试观点：SD-TP-03
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('does not recurse into support subdirs of a skill dir (references, assets, node_modules)')`
- 用例标题：references/assets/scripts/node_modules 等支持子目录中的 .md 不应被当作独立技能
- 前置条件：技能目录下存在 `SKILL.md`，且 `references/`、`assets/`、`scripts/node_modules/pkg/` 下各有 .md 文件
- 测试数据：见对应脚本
- 操作步骤：调用 `resolvePaths`
- 预期结果：claude 平台结果长度仍为 1（只有顶层 SKILL.md）

### SD-TC-13 隐藏子目录整体跳过
- 关联测试观点：SD-TP-03
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('skips hidden subdirectories but recurses into non-hidden collection dirs')`
- 用例标题：以 `.` 开头的隐藏子目录不应被递归扫描，非隐藏收集目录（如嵌套 skills/skills）应继续递归
- 前置条件：
  - `.claude/skills/skills/nested-skill/SKILL.md`（非隐藏嵌套目录，应发现）
  - `.claude/skills/.windsurf/skills/hidden-skill/SKILL.md`（隐藏目录，应跳过）
- 操作步骤：调用 `resolvePaths` 并检查 filePath 集合
- 预期结果：
  - 结果中包含 `nested-skill`
  - 结果中不包含 `hidden-skill`

### SD-TC-14 无主文件时不误将子目录当作技能
- 关联测试观点：SD-TP-03
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('does not treat reference sub-subdirectories as skills when skill dir has no main file')`、`it('does not recurse into sub-subdirectories of support dirs when skill has no main file')`
- 用例标题：technicalDir 下无顶层 .md，仅 references 子目录含多份 step 文件时不应产生技能
- 前置条件：`project-analysis/references/step-1.md`、`step-2.md`、`step-3.md`（无顶层 SKILL.md），及更深一层 `references/step-N/guide.md` 场景
- 操作步骤：调用 `resolvePaths`
- 预期结果：claude 平台结果长度为 0

### SD-TC-15 Cost-only 路径默认不扫描
- 关联测试观点：SD-TP-04
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('returns AGENTS.md once for codex and once for opencode by default')`、`it('includes AGENTS.md for Copilot and Windsurf in cost discovery mode')`
- 用例标题：默认（不传 includeCostPaths）不扫描仅用于成本估算的路径
- 前置条件：写入项目根 `AGENTS.md`
- 操作步骤：
  1. 调用 `resolvePaths(cwd, { homeDir })`（默认）
  2. 调用 `resolvePaths(cwd, { homeDir, includeCostPaths: true })`
- 预期结果：
  - 默认调用下 AGENTS.md 只归属 `codex` 与 `opencode` 两个平台
  - `includeCostPaths: true` 时额外归属 `copilot`、`windsurf`

### SD-TC-16 Override 文件优先级（Codex AGENTS.override.md）
- 关联测试观点：SD-TP-06
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('prefers Codex AGENTS.override.md over AGENTS.md in the same directory')`
- 用例标题：同目录下 AGENTS.override.md 优先于 AGENTS.md 被 codex 采用
- 前置条件：
  - `<cwd>/AGENTS.md`、`<cwd>/AGENTS.override.md`
  - `<home>/.codex/AGENTS.md`、`<home>/.codex/AGENTS.override.md`
- 操作步骤：调用 `resolvePaths(cwd, { homeDir, includeCostPaths: true })`，过滤 `platform === 'codex'`
- 预期结果：
  - 不包含 `<cwd>/AGENTS.md`
  - 包含 `<cwd>/AGENTS.override.md`
  - 不包含 `<home>/.codex/AGENTS.md`
  - 包含 `<home>/.codex/AGENTS.override.md`

### SD-TC-17 不存在路径不抛异常
- 关联测试观点：SD-TP-10
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('skips non-existent directories without throwing')`
- 用例标题：resolvePaths 面对全空目录树不应抛错
- 前置条件：仅创建空的 tempRoot，不写入任何技能文件
- 操作步骤：调用 `resolvePaths(cwd, { homeDir })`
- 预期结果：
  - 调用不抛异常
  - 返回结果为空数组

### SD-TC-18 extraPaths 支持 `~` 展开
- 关联测试观点：SD-TP-07
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('resolves tilde in extra paths')`
- 用例标题：extraPaths 中的 `~/my-skills` 应正确展开为用户主目录路径
- 前置条件：`<home>/my-skills/tool-a/SKILL.md`
- 测试数据：`extraPaths: ['~/my-skills']`
- 操作步骤：调用 `resolvePaths(cwd, { homeDir, extraPaths: ['~/my-skills'] })`
- 预期结果：
  - `platform === 'unknown'` 的结果长度为 1
  - filePath 包含 `tool-a`

### SD-TC-19 extraPaths 通配符展开（单段/多段/无匹配）
- 关联测试观点：SD-TP-07
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/resolvePaths.test.ts` → `it('expands wildcard in extra paths and scans each match')`、`it('expands multiple wildcards in a single extra path')`、`it('returns nothing for a glob that matches no directories')`
- 用例标题：extraPaths 中含 `*` 的路径应按通配符匹配展开为多个实际目录
- 前置条件：
  - `<home>/.openclaw/workspace-alpha/skills/deploy/SKILL.md`
  - `<home>/.openclaw/workspace-beta/skills/monitor/SKILL.md`
  - `<home>/.openclaw/config/skills/should-not-match/SKILL.md`
- 测试数据：
  - 用例 A：`extraPaths: ['~/.openclaw/workspace-*/skills']`
  - 用例 B：`extraPaths: ['~/agents/*/skills']`（多段通配符场景，另建两个 agent 目录）
  - 用例 C：`extraPaths: ['~/nonexistent-*/skills']`（无匹配）
- 操作步骤：分别调用 `resolvePaths` 并统计 `platform === 'unknown'` 的结果
- 预期结果：
  - 用例 A：长度为 2，分别包含 deploy 与 monitor，不包含 should-not-match
  - 用例 B：长度为 2
  - 用例 C：长度为 0

### SD-TC-20 scanSources 展示内置路径即使不存在
- 关联测试观点：SD-TP-08
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/config/scanSources.test.ts` → `it('shows builtin paths even when they do not exist')`
- 用例标题：loadEffectiveScanSources 应展示所有内置扫描源，即使对应目录不存在
- 前置条件：全新临时 home 目录，仅创建空 project 目录
- 操作步骤：调用 `loadEffectiveScanSources(projectDir, { homeDir })`
- 预期结果：
  - 结果中包含 `{ platform: 'claude', resource: 'skill', path: '~/.claude/skills', origin: 'builtin', status: 'missing' }`
  - 结果中包含 `{ platform: 'codex', resource: 'plugin', origin: 'builtin' }`

### SD-TC-21 scanSources 用户覆盖与自定义源按 id 合并
- 关联测试观点：SD-TP-08
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/config/scanSources.test.ts` → `it('merges builtin overrides and user entries by stable id')`
- 用例标题：用户配置对内置源的禁用覆盖与新增自定义源应正确合并
- 前置条件：
  - 存在自定义目录 `customDir`
  - 用户配置：禁用 `builtin-claude-skill-global-1`，新增 `user-custom` 指向 `customDir`
- 操作步骤：
  1. `saveUserConfig` 写入上述 scanSources 配置
  2. 调用 `loadEffectiveScanSources(projectDir, { homeDir })`
- 预期结果：
  - 结果包含 `{ id: 'builtin-claude-skill-global-1', enabled: false, origin: 'override' }`
  - 结果包含 `{ id: 'user-custom', resolvedPath: customDir, status: 'exists', origin: 'user' }`

### SD-TC-22 scanSources 配置校验（异常输入）
- 关联测试观点：SD-TP-08
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/config/scanSources.test.ts` → `it('validates agent, scope, duplicate ids and MCP format')`
- 用例标题：validateScanSourcesConfig 应对非法配置抛出对应错误信息
- 前置条件：无
- 测试数据与预期结果：
  | 输入 | 预期抛出的错误关键字 |
  | --- | --- |
  | `{ alien: {} }` | `不支持的 Agent` |
  | `{ claude: { tools: [] } }` | `不支持的资源类型` |
  | `{ claude: { skills: [{ id: 'x', path: '/x', scope: 'other' }] } }` | `scope 无效` |
  | `{ claude: { mcp: [{ id: 'x', path: '/x', scope: 'global', format: 'yaml' }] } }` | `MCP format` |
  | `{ claude: { skills: [{id:'x',...}, {id:'x',...}] } }`（重复 id） | `重复 id` |
- 操作步骤：分别调用 `validateScanSourcesConfig(输入)`
- 预期结果：均抛出包含对应关键字的异常

### SD-TC-23 detectAgents 优先 project 级配置
- 关联测试观点：SD-TP-09
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/detectAgents.test.ts` → `it('prefers agents configured in the current project over global-only agents')`
- 用例标题：project 级已配置的 Agent 应标记为 recommended，仅 global 级配置的不应
- 前置条件：
  - `<project>/.codex/AGENTS.md` 存在（project 级）
  - `<home>/.claude/CLAUDE.md` 存在（仅 global 级）
- 操作步骤：调用 `detectAgents(projectDir, { homeDir })`
- 预期结果：
  - 结果包含 `{ platform: 'codex', projectDetected: true, recommended: true }`
  - 结果包含 `{ platform: 'claude', globalDetected: true, projectDetected: false, recommended: false }`

### SD-TC-24 detectAgents 无已知配置路径时不报告
- 关联测试观点：SD-TP-09
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/discovery/detectAgents.test.ts` → `it('does not report agents without any known configuration path')`
- 用例标题：全新空目录下 detectAgents 应返回空数组
- 前置条件：project 与 home 目录均为空
- 操作步骤：调用 `detectAgents(projectDir, { homeDir })`
- 预期结果：返回空数组 `[]`

### SD-TC-25 frontmatter 字段解析（含块级标量与内联数组）
- 关联测试观点：SD-TP-11
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/parsing/parseSkill.test.ts` → `it('parses folded block scalar frontmatter values')`、`it('parses literal block scalar frontmatter values')`、`it('extracts inline frontmatter arrays for paths and globs')`、`it('extracts when_to_use and paths from official Claude frontmatter')`
- 用例标题：parseSkill 正确解析 name/description/author/repository/globs/paths/applyTo，包括 `>` 折叠与 `|` 字面量块级标量、内联数组 `[a, b]`
- 前置条件：构造包含以下 frontmatter 的 SKILL.md：
  ```
  ---
  name: sample-skill
  description: >
    first line
    second line
  globs: [src/**/*.ts, tests/**/*.ts]
  author: Alice
  repository: https://example.com/sample.git
  ---
  ```
- 操作步骤：调用 `parseSkill(file)`
- 预期结果：
  - `name === 'sample-skill'`
  - `description` 为折叠后的单行/段落文本（"first line second line"）
  - `triggers` 中包含两个 glob 值
  - `provenance.author === 'Alice'`，`provenance.repository` 为对应 URL（在无 git 仓库时）

### SD-TC-26 name/description 回退优先级
- 关联测试观点：SD-TP-12
- 自动化状态：✅ 已自动化（已有 + 本轮新增第 3 组）
- 测试脚本：
  - 组 1（metadata.json 回退）：`tests/parsing/parseSkill.test.ts` → `it('falls back to sibling manifest metadata for SKILL.md files')`
  - 组 2（正文 Heading/Description 回退）：`tests/parsing/parseSkill.test.ts` → `it('falls back to the heading and body when frontmatter is missing')`
  - 组 3（纯正文截断回退，**本轮新增**）：`tests/parsing/parseSkill.test.ts` → `it('falls back to the first 200 chars of body when no frontmatter, metadata, or heading exist')`
- 用例标题：frontmatter 缺失时依次回退到 metadata.json → 正文 Heading → 文件名
- 前置条件：分三组构造：
  1. 无 frontmatter，有 `metadata.json` 含 name/description
  2. 无 frontmatter、无 metadata.json，正文含 `# Heading` 与 `## Description` 小节
  3. 全部缺失，仅有正文内容（无标题、无 metadata 文件）
- 操作步骤：对每组分别调用 `parseSkill`
- 预期结果：
  - 组 1：`name`/`description` 取自 metadata.json
  - 组 2：`name` 取自 `# Heading`，`description` 取自正文内容
  - 组 3：`name` 回退为文件名（非 SKILL.md 文件名场景），`description` 回退为正文前 200 字符
- 新增测试代码（组 3）：
  ```ts
  // SD-TC-26
  it('falls back to the first 200 chars of body when no frontmatter, metadata, or heading exist', async () => {
    const filePath = join(tmpdir(), `skill-doctor-body-only-${Date.now()}.md`);
    const body = 'A'.repeat(250);
    writeFileSync(filePath, body, 'utf8');

    const result = await parseSkill(makeSkillFile(filePath));

    expect(result).not.toBeNull();
    expect(result?.name).toBe(basename(filePath));
    expect(result?.description).toBe('A'.repeat(200));
  });
  ```

### SD-TC-27 触发词提取去重
- 关联测试观点：SD-TP-13
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/parsing/parseSkill.test.ts` → `it('deduplicates triggers sourced from multiple frontmatter and body fields')`
- 用例标题：triggers 应合并 When to Use/Trigger 小节 bullet、frontmatter when_to_use/paths/globs/applyTo/description，并去重
- 前置条件：SKILL.md 同时包含 `## When to Use` 中的 bullet、frontmatter 中重复出现的同一字符串
- 操作步骤：调用 `parseSkill`
- 预期结果：`triggers` 数组中无重复项，且包含所有来源的唯一值
- 新增测试代码：
  ```ts
  // SD-TC-27
  it('deduplicates triggers sourced from multiple frontmatter and body fields', async () => {
    const filePath = join(tmpdir(), `skill-doctor-dedup-triggers-${Date.now()}.md`);
    writeFileSync(
      filePath,
      [
        '---',
        'name: dedup-triggers',
        'description: shared trigger text',
        'when_to_use: shared trigger text',
        '---',
        '',
        '## When to Use',
        '',
        '- shared trigger text',
        '- unique bullet trigger',
      ].join('\n'),
      'utf8',
    );

    const result = await parseSkill(makeSkillFile(filePath));

    expect(result).not.toBeNull();
    const occurrences = result?.triggers.filter((trigger) => trigger === 'shared trigger text');
    expect(occurrences).toHaveLength(1);
    expect(result?.triggers).toContain('unique bullet trigger');
  });
  ```

### SD-TC-28 provenance 不泄漏工作区自身 git 信息
- 关联测试观点：SD-TP-14
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/parsing/parseSkill.test.ts` → `it('does not inherit enclosing workspace git provenance for nested skill directories')`；`tests/cli/integration.test.ts` → `it('scan, show, and audit prefer skill metadata without leaking workspace git provenance')`
- 用例标题：技能目录本身没有独立 `.git` 时，不应误用工作区仓库的 git 归属信息覆盖 frontmatter/metadata 提供的作者与仓库
- 前置条件：
  - 技能位于当前 git 仓库工作区内（`cwd` 本身处于 monorepo 的 git 控制下）
  - SKILL.md frontmatter 提供 `author: Frontmatter Author`、`repository: https://example.com/frontmatter.git`
  - `metadata.json` 提供 `author.name: Metadata Author`、`repository.url: https://github.com/example/provenance-skill.git`
- 操作步骤：执行 `scan`/`show`/`audit` 并检查 provenance 字段
- 预期结果：
  - provenance 优先取 metadata.json 的值（因技能目录无自身 `.git`，git 信息应为空，不发生覆盖）
  - 不应出现属于外层工作区仓库的 URL 或作者名

### SD-TC-29 provenance 缓存命中避免重复解析
- 关联测试观点：SD-TP-15
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/parsing/parseSkill.test.ts` → `it('reuses cached provenance before falling back to the analysis LLM')`；`tests/cli/integration.test.ts` → `it('scan --json reuses cached provenance between runs')`
- 用例标题：相同 sourcePath 的 provenance 缓存应命中，不重复触发 LLM 提取
- 前置条件：已配置 analysis LLM，且首次扫描后已保存 provenance 缓存至 `~/.skill-doctor/provenance.json`
- 操作步骤：
  1. 第一次执行 `scan --json`（触发 LLM 补全 provenance 并写入缓存）
  2. 第二次执行 `scan --json`（缓存应命中）
- 预期结果：
  - 两次结果的 provenance 数据一致
  - 第二次执行不应产生新的 LLM 调用（CLI 集成用例中通过本地 HTTP mock server 的 `requestCount` 断言仅为 1）

### SD-TC-30 provenance 缓存文件损坏容错
- 关联测试观点：SD-TP-15
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/parsing/provenanceCache.test.ts` → `it('returns empty map instead of throwing when the cache file has malformed JSON')`
- 用例标题：provenance.json 内容非法 JSON 或缺少 BOM 时 loadProvenanceCache 不应抛异常
- 前置条件：手工写入非法 JSON 内容到缓存文件路径
- 操作步骤：调用 `loadProvenanceCache(cachePath)`
- 预期结果：返回空 Map，不抛异常
- 新增测试代码：
  ```ts
  // SD-TC-30
  it('returns empty map instead of throwing when the cache file has malformed JSON', () => {
    writeFileSync(cachePath, '{ not valid json', 'utf8');

    expect(() => loadProvenanceCache(cachePath)).not.toThrow();
    expect(loadProvenanceCache(cachePath).size).toBe(0);
  });
  ```

### SD-TC-31 空 SKILL.md 文件被跳过
- 关联测试观点：SD-TP-16
- 自动化状态：✅ 已自动化（已有）
- 测试脚本：`tests/parsing/parseSkill.test.ts` → `it('returns null for empty files')`
- 用例标题：内容为空（仅空白字符）的 SKILL.md 应被 parseSkill 判定跳过
- 前置条件：写入内容为空字符串或纯空白的 SKILL.md
- 操作步骤：调用 `parseSkill(file)`
- 预期结果：返回值为 `null`

### SD-TC-32 健康检查编排的阶段进度事件
- 关联测试观点：SD-TP-17
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/application/runHealthCheck.test.ts` → `it('emits progress events in discovering -> conflicts -> audit -> context -> grouping -> complete order')`
- 用例标题：runHealthCheck 应按 discovering→conflicts→audit→context→grouping→complete 顺序触发 onProgress 回调
- 前置条件：构造包含至少一个技能的工作区
- 操作步骤：
  1. 调用 `runHealthCheck(options, onProgress)`，记录每次回调的 phase 顺序
- 预期结果：
  - phase 序列与预期顺序一致
- 新增测试代码：
  ```ts
  // SD-TC-32
  it('emits progress events in discovering -> conflicts -> audit -> context -> grouping -> complete order', async () => {
    const root = createTempRoot();
    const projectDir = join(root, 'project');
    const homeDir = join(root, 'home');
    writeFile(
      join(projectDir, '.claude', 'skills', 'sample-skill', 'SKILL.md'),
      ['---', 'name: sample-skill', 'description: sample skill for progress tracking', '---'].join('\n'),
    );

    const phases: string[] = [];
    await runHealthCheck({ projectDir, homeDir, scope: 'project' }, (event) => {
      phases.push(event.phase);
    });

    expect(phases).toEqual(['discovering', 'conflicts', 'audit', 'context', 'grouping', 'complete']);

    cleanupTempRoots();
  });
  ```

### SD-TC-33 健康检查中断信号生效
- 关联测试观点：SD-TP-17
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/application/runHealthCheck.test.ts` → `it('rejects with an AbortError when the signal is already aborted')`
- 用例标题：传入已 aborted 的 AbortSignal 时，runHealthCheck 应在下一个检查点抛出 AbortError
- 前置条件：构造一个 `AbortController`，在调用前即 `controller.abort()`
- 操作步骤：调用 `runHealthCheck({ ...options, signal: controller.signal })`
- 预期结果：
  - Promise 被 reject
  - 抛出的异常 `name === 'AbortError'`
- 新增测试代码：
  ```ts
  // SD-TC-33
  it('rejects with an AbortError when the signal is already aborted', async () => {
    const root = createTempRoot();
    const projectDir = join(root, 'project');
    const homeDir = join(root, 'home');
    const controller = new AbortController();
    controller.abort();

    await expect(
      runHealthCheck({ projectDir, homeDir, scope: 'project', signal: controller.signal }),
    ).rejects.toMatchObject({ name: 'AbortError' });

    cleanupTempRoots();
  });
  ```

### SD-TC-34 scan 文本输出与 --json 输出的数据口径一致性
- 关联测试观点：SD-TP-21
- 自动化状态：✅ 已自动化（本轮新增）
- 测试脚本：`tests/cli/integration.test.ts` → `it('scan text output and scan --json report the same skill/duplicate/conflict counts')`
- 用例标题：同一份扫描结果下，scan 文本模式与 --json 模式的技能数/重复数/冲突数/平台计数/技能名应完全一致
- 前置条件：构造包含跨 scope 重复技能（触发 duplicate）与语义冲突技能（触发 conflict）的工作区
- 测试数据：
  - project：`git-workflow`、`github-automation`（描述高度重叠，触发 conflict）
  - global：`git-workflow`（与 project 同名，触发 duplicate）
- 操作步骤：
  1. 执行 `skill-doctor scan`（文本模式）
  2. 执行 `skill-doctor scan --json`
  3. 用 JSON 结果中的 summary 数值与 skills 名称，反向断言文本输出中包含对应字符串
- 预期结果：
  - 文本输出中的 `Total skills installed:`、`Duplicates detected:`、`Conflicts detected:` 数值与 JSON `summary` 字段一致
  - 文本输出中每个平台的计数行与 JSON `summary.platforms` 一致
  - 文本输出中包含 JSON `skills` 数组里每个技能的名称
- 验证说明：该用例依赖预编译后的 CLI（`dist/index.cjs`），在 `tests/cli/integration.test.ts` 的 `describe.skipIf(process.platform === 'win32')` 套件内。本轮在 Windows 开发机上临时移除 skip 条件单独跑通验证（`npx vitest run tests/cli/integration.test.ts -t "scan text output and scan --json report the same"` → 1 passed），随后已还原 skip 逻辑，正式执行会在 CI 的 ubuntu-latest 环境完成。
- 新增测试代码：
  ```ts
  // SD-TC-34
  it('scan text output and scan --json report the same skill/duplicate/conflict counts', () => {
    const root = createTempRoot();
    const cwd = join(root, 'workspace');
    const home = join(root, 'home');

    writeFile(
      join(cwd, '.claude', 'skills', 'git-workflow', 'SKILL.md'),
      ['---', 'name: git-workflow', 'description: manage git workflow, branches, commits, and pull requests', '---', '', '## When to Use', '', '- create branch', '- write commit message', '- open pull request'].join('\n'),
    );
    writeFile(
      join(cwd, '.claude', 'skills', 'github-automation', 'SKILL.md'),
      ['---', 'name: github-automation', 'description: automate git workflow, branch creation, commit messages, and pull request handling', '---', '', '## When to Use', '', '- create branch', '- write commit message', '- open pull request'].join('\n'),
    );
    writeFile(
      join(home, '.claude', 'skills', 'git-workflow', 'SKILL.md'),
      ['---', 'name: git-workflow', 'description: manage git workflow, branches, commits, and pull requests', '---', '', '## When to Use', '', '- create branch', '- write commit message', '- open pull request'].join('\n'),
    );

    const text = runCli(['scan'], cwd, home);
    const json = JSON.parse(runCli(['scan', '--json'], cwd, home).stdout);

    expect(text.status).toBe(0);
    expect(text.stdout).toContain(`Total skills installed: ${json.summary.totalSkillsInstalled}`);
    expect(text.stdout).toContain(`Duplicates detected: ${json.summary.duplicatesDetected}`);
    expect(text.stdout).toContain(`Conflicts detected: ${json.summary.conflictsDetected}`);
    for (const [platform, count] of Object.entries(json.summary.platforms as Record<string, number>)) {
      expect(text.stdout).toContain(`- ${platform}: ${count}`);
    }
    for (const skill of json.skills as { name: string }[]) {
      expect(text.stdout).toContain(`- ${skill.name}`);
    }
  });
  ```

## 4. 自动化覆盖总览

| 用例编号 | 自动化状态 | 脚本文件 |
| --- | --- | --- |
| SD-TC-01~04, 06~09 | ✅ 已有 | `tests/cli/integration.test.ts` |
| SD-TC-05 | ✅ 本轮新增 | `tests/cli/integration.test.ts` |
| SD-TC-10~19 | ✅ 已有 | `tests/discovery/resolvePaths.test.ts` |
| SD-TC-20~22 | ✅ 已有 | `tests/config/scanSources.test.ts` |
| SD-TC-23~24 | ✅ 已有 | `tests/discovery/detectAgents.test.ts` |
| SD-TC-25 | ✅ 已有 | `tests/parsing/parseSkill.test.ts` |
| SD-TC-26 | ✅ 已有 + 本轮新增（组3） | `tests/parsing/parseSkill.test.ts` |
| SD-TC-27 | ✅ 本轮新增 | `tests/parsing/parseSkill.test.ts` |
| SD-TC-28~29 | ✅ 已有 | `tests/parsing/parseSkill.test.ts`、`tests/cli/integration.test.ts` |
| SD-TC-30 | ✅ 本轮新增 | `tests/parsing/provenanceCache.test.ts` |
| SD-TC-31 | ✅ 已有 | `tests/parsing/parseSkill.test.ts` |
| SD-TC-32~33 | ✅ 本轮新增 | `tests/application/runHealthCheck.test.ts` |
| SD-TC-34 | ✅ 本轮新增 | `tests/cli/integration.test.ts` |

本模块 34 条用例**全部**已自动化，无遗留的"需人工复核"项。本轮共新增 7 个自动化测试用例（SD-TC-05、SD-TC-26 组3、SD-TC-27、SD-TC-30、SD-TC-32、SD-TC-33、SD-TC-34），已全部通过：

```
npx vitest run tests/application/runHealthCheck.test.ts tests/parsing/parseSkill.test.ts tests/parsing/provenanceCache.test.ts
✓ tests/parsing/provenanceCache.test.ts (5 tests)
✓ tests/parsing/parseSkill.test.ts (18 tests)
✓ tests/application/runHealthCheck.test.ts (7 tests)

# tests/cli/integration.test.ts 依赖预编译 CLI，在 describe.skipIf(win32) 下本机跳过；
# 已临时移除 skip 条件单独验证 SD-TC-34：
npx vitest run tests/cli/integration.test.ts -t "scan text output and scan --json report the same"
✓ tests/cli/integration.test.ts (1 test)
```

`tests/cli/integration.test.ts` 中新增的 SD-TC-05、SD-TC-34 在 Windows 开发机上因 `describe.skipIf(process.platform === 'win32')` 被跳过（该套件依赖预编译产物以子进程方式验证真实 CLI 行为），会在 CI（`.github/workflows/quality.yml`，`ubuntu-latest`）中执行。

## 5. 用例补充字段
- 实际结果：____________
- 是否通过：通过 / 不通过
- 缺陷编号：____________
- 备注：____________
