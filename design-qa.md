# 第三版「优化建议」实现验收

日期：2026-09-18

final result: passed

## 比较对象与证据

- 源视觉：`/Users/evilstar/.codex/generated_images/01a0afc5-ef02-7d03-8477-2c0ec8200204/exec-9483ed8f-0f6c-4ba6-bade-15cce96e1deb.png`，1487 × 1058 px。
- 本地实现：`http://127.0.0.1:61011/#/context?view=recommendations`，需本地服务的会话 cookie；未部署到公网。
- 桌面同项选择：`docs/optimization-qa/desktop-final.png`，1477 × 1048 px；CSS viewport 1488 × 1056。
- 默认可执行建议：`docs/optimization-qa/desktop-delivery.png`。
- 手机：`docs/optimization-qa/mobile-final.png`，379 × 1846 px 全页；CSS viewport 390 × 844。
- 手机实际确认视口：`docs/optimization-qa/mobile-confirm.png`，379 × 820 px；非全页截图，确认按钮与底部导航均可见。
- 平板：`docs/optimization-qa/tablet.png`，823 × 1361 px 全页；CSS viewport 834 × 1112。
- 截图捕获省略浏览器滚动条沟槽，CSS clientWidth 分别为 1477/379/823，scrollWidth 均与 clientWidth 相等。按内容区域近 1:1 比较，无双倍密度缩放；未把滚动条尺寸差异计作视觉缺陷。
- 源图和实现截图在同一次比较输入中共同打开，分别进行了首次、修正后与最终比较。标题、选项、收益、影响文案在原始分辨率可读，因此无需裁剪放大才可判断的细节区域；另以手机确认区独立视口检查触控与按钮遮挡。

状态差异：源图为示例数字且技能目录可关闭；本机项目已关闭目录，因此该项显示 0 Token 与不可重复执行。真实可执行默认项是记忆，展示该会话实测文本估算 3874 Token。两种选中状态都已截图。没有为了还原示例金额而伪造运行数据。

## Findings 与修复记录

1. [P2，已修复] 首次实现沿用双层工具栏后，确认按钮落到桌面首屏外。
   - 证据：`docs/optimization-qa/desktop-before.png`；源图首屏可见主操作。
   - 修复：仅缩短优化页顶部、选项及区块间距，不更改全站设计 tokens。
   - 复验：`desktop-after.png`、`desktop-final.png`，主操作已回到首屏，收益与影响仍保持左右两列。
2. [P2，已修复] 同一 task 的多个 rollout 造成重复选项与 React 重复 key。
   - 修复：按最新活动顺序每 task 保留一个日志，新增重复会话回归测试。
   - 复验：最终列表去重。控制台只保留 13:40 UTC 的旧构建错误，修正后没有新 warn/error。
3. [P2，已修复] 手机会话选择器加刷新按钮超过行宽，刷新落到下一行。
   - 修复：可伸缩选择器、44px 刷新触控区、价格说明另起一行。
   - 复验：`mobile-final.png`；无页面横向溢出，手机确认区可正常滚动到达，底部导航不遮挡主按钮。
4. [P2，已修复] 初次实现为费用页面重建全部历史上下文，读取明显等待。
   - 修复：只解析任务用量，跳过非当前项目正文；独立读取原始初始头，不降低移除验证条件。
   - 复验：本机真实 19 条任务结果读取 3498ms；新增完整/轻量解析用量一致性与项目隔离测试。这是一次本机测量，不是性能 SLA。

最终比较无待处理 P0/P1/P2。

## 必查视觉表面

| 表面 | 结论 |
| --- | --- |
| 字体与排版 | 保留项目 Rubik / 系统中文字体栈；34px 主标题、26px 二级标题、46px Token 数字与 12–16px 辅助文字形成层级。手机适当缩小标题，长路径断行。未复制生成图中的字体伪影。 |
| 布局与节奏 | 三步导航、两条单选建议、收益/影响双列、主次操作顺序与第三版一致。现有多 Agent 工具栏及侧栏宽度保留，是生产外壳约束；经间距修正不再挤走桌面主操作。手机纵向排布，平板保留双列。 |
| 色彩与 tokens | 使用现有深紫背景、暖紫按钮、浅紫选中边框、黄绿色收益、橙色全局影响。没有新增主题或页面硬编码调色板。与源图色温的小差异来自既有固定风格契约，接受。 |
| 图片与图标 | 沿用现有 Skill Doctor 标识资产、平台资产与 Lucide 图标；不使用新造 SVG 图替代品牌图像，无插画/照片占位符。图标描边、对齐和标签一致。 |
| 文案与状态 | 模拟数字换为真实证据；配置写入不等于生效；美元是内置表的 API 等价估算。单技能与推荐插件限制有明确说明。未知数据、加载、空状态、错误、已关闭、待验证、仍存在、已移除、撤销均有状态区分。 |
| 可访问性 | 单选 fieldset、显式 label、步骤导航 aria-current、错误 alert、验证 live region、原生键盘控件。沿用 focus-visible 与 reduced-motion；手机确认按钮及选择器已实际操作。未做完整屏幕阅读器审计。 |

## 功能验收

- 浏览器：一级入口、可用建议默认选择、会话切换、步骤切换、会话累计费用、限制说明、手机确认面板。
- 真实预览：未勾选全局影响时确认按钮禁用，勾选后启用；随后选择「稍后再说」。没有点击真实配置提交。
- 隔离测试：项目目录/用户目录准确落点、摘要过期拒绝、项目覆盖拒绝、未知版本拒绝、symlink 拒绝、原子写入、新任务头 absent/present/unknown、配置漂移拒绝、仅目标键撤销、认证和跨项目 API 拒绝。
- `npm test -- --maxWorkers=2`：87 个文件、693 项通过。初始受限环境 HTTP 监听失败，开放本机监听后重跑；降低并行数避免既有 tokenizer 测试的 5 秒争用超时。
- `npm run typecheck:ui`、`npm run build`、`git diff --check` 通过。构建仍有 Vite 大于 500KB 的非阻塞 bundle 提示。
- 后续资源抽屉只读标签调整另跑 UI 定向回归与 UI 构建；没有删除用户已有主题改动。

## 边界与后续

- 此轮没有再次修改用户真实配置并新建 Desktop task；兼容性依据是先前调查的真实新 task 结果，新实现写入与验证算法通过隔离文件测试。不得把此轮 UI 验收称为新的 Desktop 端到端实验。
- 只开放调查版本的两项控制，不宣称单个技能、推荐插件或任意 block 都可禁用。
- 页面只保存最近一项操作进度，暂未提供完整操作历史列表和价格/时间窗编辑器。
- P3：后续可优化 bundle 拆分与更细的读取进度；不影响当前流程。
- 截图含本机用量信息，保存在 ignored `docs/`，不自动发布。

## Implementation Checklist

- [x] 第三版结构与固定主题。
- [x] 真实用量与估算边界。
- [x] 预览、全局影响确认、待验证与单键撤销。
- [x] 桌面、平板、手机视觉与交互验收。
- [x] 修复重复 task、首屏操作位置、手机刷新与读取性能。
- [x] 双语说明、回归测试及构建。

## 2026-09-18 累计收益展示调整

- 累计 Token 与金额改为主视觉，首轮收益缩小放在旁边；会话 turn 数与模型响应数以辅助文字展示。手机窄屏自动换行。
- 按去重后的各次响应累加可确认的目标上下文，结合各响应缓存边界和模型价格估算金额。压缩、日志缺口及未知价格不外推；覆盖不全时明确标识部分收益和可估价响应数。
- 真实历史样本：上下文与用量覆盖 45 / 65 次响应，累计估算 174,690 Token；首轮 3,882 Token。金额只涵盖其中 27 次可估价响应，不代表实际账单节省。
- 桌面 1488×1056、手机 390×844 浏览器验收通过；手机页面无横向溢出。截图：`docs/optimization-qa/cumulative-desktop.png`、`docs/optimization-qa/cumulative-mobile.png`（本机保存，不发布）。
- `npm test -- --maxWorkers=2`：87 个文件、695 项通过；`npm run typecheck:ui`、`npm run build`、`git diff --check` 通过。
- 没有修改用户真实 Codex 配置，也没有将此轮验收称为新的 Desktop 配置生效实验。
